[gcode_macro G28]
rename_existing: G28.1
gcode:
    RUN_SHELL_COMMAND CMD=SOUND_HOME
    RUN_SHELL_COMMAND CMD=WLED_HOMING

    {% if not rawparams %}
        # Home X and Y together
        G28.1 X Y

        # Lift after XY homing
        G90
        G1 Z10 F1800

        # Move to front-left (or wherever you prefer)
        G1 X10 Y10 F6000

        # Home Z
        G28.1 Z
    {% else %}
        # Preserve behavior for G28 X, G28 Y, G28 Z, etc.
        G28.1 {rawparams}
    {% endif %}

    RUN_SHELL_COMMAND CMD=WLED_STARTUP

[gcode_macro BED_MESH_CALIBRATE]
rename_existing: BED_MESH_CALIBRATE_BASE
gcode:
    BED_MESH_CALIBRATE_BASE
    RUN_SHELL_COMMAND CMD=SOUND_MESH